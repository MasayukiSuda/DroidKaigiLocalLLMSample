name: Android CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Force Gradle 8.13 and clear cache
        run: |
          echo "=== Before update ==="
          grep "distributionUrl" gradle/wrapper/gradle-wrapper.properties
          ./gradlew --version || true
          
          echo "=== Clearing Gradle cache ==="
          rm -rf ~/.gradle/caches || true
          rm -rf ~/.gradle/wrapper || true
          rm -rf .gradle || true
          
          echo "=== Updating to Gradle 8.13 ==="
          sed -i.bak 's|gradle-.*-bin\.zip|gradle-8.13-bin.zip|' gradle/wrapper/gradle-wrapper.properties
          
          echo "=== After update ==="
          grep "distributionUrl" gradle/wrapper/gradle-wrapper.properties
          
          echo "=== Downloading fresh Gradle ==="
          ./gradlew --version
          
          echo "=== Verifying Gradle version ==="
          ./gradlew --version | grep "Gradle 8.13" || (echo "ERROR: Gradle 8.13 not detected" && exit 1)

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-v3-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-v3-

      - name: Validate Gradle wrapper
        uses: gradle/wrapper-validation-action@v2

      - name: Run unit tests
        run: ./gradlew testDebugUnitTest

      - name: Build with Gradle
        run: ./gradlew assembleDebug

      - name: Upload build outputs (APKs)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build-outputs
          path: app/build/outputs/

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: app/build/test-results/

  android-test:
    runs-on: macos-14
    env:
      MD_DEVICE_TASK: ":app:pixel7Api34DebugAndroidTest"

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Force Gradle 8.13 and clear cache
        run: |
          echo "=== Before update ==="
          grep "distributionUrl" gradle/wrapper/gradle-wrapper.properties
          ./gradlew --version || true
          
          echo "=== Clearing Gradle cache ==="
          rm -rf ~/.gradle/caches || true
          rm -rf ~/.gradle/wrapper || true
          rm -rf .gradle || true
          
          echo "=== Updating to Gradle 8.13 ==="
          sed -i.bak 's|gradle-.*-bin\.zip|gradle-8.13-bin.zip|' gradle/wrapper/gradle-wrapper.properties
          
          echo "=== After update ==="
          grep "distributionUrl" gradle/wrapper/gradle-wrapper.properties
          
          echo "=== Downloading fresh Gradle ==="
          ./gradlew --version
          
          echo "=== Verifying Gradle version ==="
          ./gradlew --version | grep "Gradle 8.13" || (echo "ERROR: Gradle 8.13 not detected" && exit 1)

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-v3-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-v3-

      - name: Verify Android SDK tools
        run: |
          echo "ANDROID_HOME=$ANDROID_HOME"
          sdkmanager --version || true
          avdmanager --help || true

      - name: List Managed Device test tasks
        run: |
          ./gradlew :app:tasks --all | grep -E "DebugAndroidTest|Managed|Device" || true
  
      - name: Accept Android SDK licenses & install components (for Managed Devices)
        run: |
          set -e
          export ANDROID_SDK_ROOT="$ANDROID_HOME"
          yes | sdkmanager --licenses
          yes | sdkmanager --install \
            "platform-tools" \
            "platforms;android-34" \
            "emulator" \
            "system-images;android-34;google_atd;arm64-v8a"

      - name: Run instrumentation tests (Gradle Managed Devices)
        run: |
          echo "Running $MD_DEVICE_TASK"
          ./gradlew --stacktrace --info -Pandroid.experimental.testOptions.managedDevices.emulator.gpu=swiftshader_indirect $MD_DEVICE_TASK

      - name: Upload Android test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: android-test-results
          path: app/build/reports/androidTests/

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Force Gradle 8.13 and clear cache
        run: |
          echo "=== Before update ==="
          grep "distributionUrl" gradle/wrapper/gradle-wrapper.properties
          ./gradlew --version || true
          
          echo "=== Clearing Gradle cache ==="
          rm -rf ~/.gradle/caches || true
          rm -rf ~/.gradle/wrapper || true
          rm -rf .gradle || true
          
          echo "=== Updating to Gradle 8.13 ==="
          sed -i.bak 's|gradle-.*-bin\.zip|gradle-8.13-bin.zip|' gradle/wrapper/gradle-wrapper.properties
          
          echo "=== After update ==="
          grep "distributionUrl" gradle/wrapper/gradle-wrapper.properties
          
          echo "=== Downloading fresh Gradle ==="
          ./gradlew --version
          
          echo "=== Verifying Gradle version ==="
          ./gradlew --version | grep "Gradle 8.13" || (echo "ERROR: Gradle 8.13 not detected" && exit 1)

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-v3-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-v3-

      - name: Run lint
        run: ./gradlew lintDebug

      - name: Upload lint reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lint-reports
          path: app/build/reports/